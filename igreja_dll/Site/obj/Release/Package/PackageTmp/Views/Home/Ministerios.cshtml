@model IEnumerable<business.classes.Abstrato.Ministerio>

@{
    ViewBag.Title = "Ministerios";
}

<h2>Ministerios</h2>

<input type="hidden" value="0" id="valor" />
<label style="background-color:aqua;">Lider de celula</label>
<label style="background-color:green;">Lider de celula em treinamento</label>
<label style="background-color:yellow;">Lider de ministério</label>
<label style="background-color:skyblue;">Lider de ministério em treinamento</label>
<label style="background-color:wheat;">Supervisor de celula</label>
<label style="background-color:bisque;">Supervisor de celula em treinamento</label>
<label style="background-color:sandybrown;">Supervisor de minitério</label>
<label style="background-color:saddlebrown;">Supervisor de minitério em treinamento</label>

<table border="1" class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Proposito)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Maximo_pessoa)
        </th>
        <th></th>
    </tr>

    @Html.AntiForgeryToken()

    @foreach (var item in Model.OfType<business.classes.Ministerio.Lider_Celula>())
    {
        <tr style="background-color:aqua;">
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proposito)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Maximo_pessoa)
            </td>
            <td>
                @Html.ActionLink("Detalhes", "Details", "Ministerio", new { id = item.IdMinisterio }, null) |

                @using (Html.BeginForm("ParticiparMinisterio", "Home", new { Id = item.IdMinisterio }, FormMethod.Post, null))
                {
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Participar" class="btn btn-default" />
                        </div>
                    </div>
                }
            </td>
        </tr>
    }

    @foreach (var item in Model.OfType<business.classes.Ministerio.Lider_Celula_Treinamento>())
    {
        <tr style="background-color:green;">
            
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proposito)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Maximo_pessoa)
            </td>
            <td>
                @Html.ActionLink("Detalhes", "Details", "Ministerio", new { id = item.IdMinisterio }, null) |
                @using (Html.BeginForm("ParticiparMinisterio", "Home", new { Id = item.IdMinisterio }, FormMethod.Post, null))
                {
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Participar" class="btn btn-default" />
                        </div>
                    </div>
                }
            </td>
        </tr>
    }

    @foreach (var item in Model.OfType<business.classes.Ministerio.Lider_Ministerio>())
    {
        <tr style="background-color:yellow;">
            
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proposito)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Maximo_pessoa)
            </td>
            <td>
                @Html.ActionLink("Detalhes", "Details", "Ministerio", new { id = item.IdMinisterio }, null) |
                @using (Html.BeginForm("ParticiparMinisterio", "Home", new { Id = item.IdMinisterio }, FormMethod.Post, null))
                {
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Participar" class="btn btn-default" />
                        </div>
                    </div>
                }
            </td>
        </tr>
    }

    @foreach (var item in Model.OfType<business.classes.Ministerio.Lider_Ministerio_Treinamento>())
    {
        <tr style="background-color:skyblue;">
            
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proposito)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Maximo_pessoa)
            </td>
            <td>
                @Html.ActionLink("Detalhes", "Details", "Ministerio", new { id = item.IdMinisterio }, null) |
                @using (Html.BeginForm("ParticiparMinisterio", "Home", new { Id = item.IdMinisterio }, FormMethod.Post, null))
                {
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Participar" class="btn btn-default" />
                        </div>
                    </div>
                }
            </td>
        </tr>
    }

    @foreach (var item in Model.OfType<business.classes.Ministerio.Supervisor_Celula>())
    {
        <tr style="background-color:wheat;">
            
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proposito)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Maximo_pessoa)
            </td>
            <td>
                @Html.ActionLink("Detalhes", "Details", "Ministerio", new { id = item.IdMinisterio }, null) |
                @using (Html.BeginForm("ParticiparMinisterio", "Home", new { Id = item.IdMinisterio }, FormMethod.Post, null))
                {
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Participar" class="btn btn-default" />
                        </div>
                    </div>
                }
            </td>
        </tr>
    }

    @foreach (var item in Model.OfType<business.classes.Ministerio.Supervisor_Celula_Treinamento>())
    {
        <tr style="background-color:bisque;">
           
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proposito)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Maximo_pessoa)
            </td>
            <td>
                @Html.ActionLink("Detalhes", "Details", "Ministerio", new { id = item.IdMinisterio }, null) |
                @using (Html.BeginForm("ParticiparMinisterio", "Home", new { Id = item.IdMinisterio }, FormMethod.Post, null))
                {
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Participar" class="btn btn-default" />
                        </div>
                    </div>
                }
            </td>
        </tr>
    }

    @foreach (var item in Model.OfType<business.classes.Ministerio.Supervisor_Ministerio>())
    {
        <tr style="background-color:sandybrown;">
            
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proposito)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Maximo_pessoa)
            </td>
            <td>
                @Html.ActionLink("Detalhes", "Details", "Ministerio", new { id = item.IdMinisterio }, null) |
                @using (Html.BeginForm("ParticiparMinisterio", "Home", new { Id = item.IdMinisterio }, FormMethod.Post, null))
                {
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Participar" class="btn btn-default" />
                        </div>
                    </div>
                }
            </td>
        </tr>
    }

    @foreach (var item in Model.OfType<business.classes.Ministerio.Supervisor_Ministerio_Treinamento>())
    {
        <tr style="background-color:saddlebrown;">
            
            <td>
                @Html.DisplayFor(modelItem => item.Nome)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Proposito)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Maximo_pessoa)
            </td>
            <td>
                @Html.ActionLink("Detalhes", "Details", "Ministerio", new { id = item.IdMinisterio }, null) |
                @using (Html.BeginForm("ParticiparMinisterio", "Home", new { Id = item.IdMinisterio }, FormMethod.Post, null))
                {
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        <div class="col-md-offset-2 col-md-10">
                            <input type="submit" value="Participar" class="btn btn-default" />
                        </div>
                    </div>
                }
            </td>
        </tr>
    }

</table>


