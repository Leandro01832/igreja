@model IEnumerable<business.classes.Cargo_Supervisor_Treinamento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Pessoa.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Maximo_celula)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Pessoa.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Maximo_celula)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Supervisortreinamentoid }) |
            @Html.ActionLink("Details", "Details", new { id=item.Supervisortreinamentoid }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Supervisortreinamentoid })
        </td>
    </tr>
}

</table>
